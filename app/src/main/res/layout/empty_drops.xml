<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <ImageView
        android:id="@+id/ivLogo"
        android:layout_width="@dimen/ivLogoSize"
        android:layout_height="@dimen/ivLogoSize"
        android:layout_centerHorizontal="true"
        android:layout_margin="@dimen/ivLogoMargin"
        android:src="@drawable/logo_large" />

    <Button
        android:id="@+id/btnAddDrop"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/ivLogo"
        android:layout_centerHorizontal="true"
        android:layout_margin="@dimen/btnAddMargin"
        android:background="@drawable/bg_btn"
        android:fontFamily="sans-serif-thin"
        android:paddingBottom="@dimen/btnAddPaddingVertical"
        android:paddingLeft="@dimen/btnAddPaddingHorizontal"
        android:paddingRight="@dimen/btnAddPaddingHorizontal"
        android:paddingTop="@dimen/btnAddPaddingVertical"
        android:text="@string/add_drop"
        android:textAllCaps="false"
        android:textColor="@color/btn_color"
        android:textSize="@dimen/btnAddFontSize" />

</RelativeLayout>